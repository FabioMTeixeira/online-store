import Navbar from '../Navbar';
import './style.css';

export default function Header() {
    return (
        <header>
            <h1>Loja Virtual</h1>
            <Navbar />
        </header>
    );
}